A rehash of the start_stop script from the community site

Examples:

$ ./start_stop.rb  stop default 127.0.0.1 ports-default admin admin
Stopping the Boss
The Boss is done!!


$ ./start_stop.rb  start default 127.0.0.1 ports-default admin admin
Server is still warming up
Server is still warming up
Server is still warming up
Server is still warming up
Server is still warming up
Server is still warming up
Server is still warming up
Server is still warming up
Server is still warming up
Server is still warming up
Server is still warming up
The Boss is playing!!
